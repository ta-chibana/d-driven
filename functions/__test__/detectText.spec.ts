// TODO: write test for detectText
// import detectText from '../src/detectText'

describe('detectText', () => {
  context('when xxx', () => {
    it('hogehoge', () => {
      expect(1 + 1).toBe(2)
    })
  })
})
